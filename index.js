const generateRandomNumbers = require('./lib/gen-random-numbers.js');
// const generateRandomLetters = require('.lib/gen-random-numbers.js');

module.exports = {
  generateRandomNumbers
  // generateRandomLetters
}